import { RegistrationForm } from "../components/RegistrationForm"

export const RegistrationPage = (): JSX.Element =>{
    return (
        <div>
            <RegistrationForm/>
        </div>
    )
}